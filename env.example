# =============================================================================
# CONFIGURACIÓN PARA COOLIFY - MEDUSA 2.0
# =============================================================================
# Copia este archivo a .env y configura los valores según tu entorno

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
POSTGRES_PASSWORD=your_secure_password_here

# =============================================================================
# BACKEND URLS & CORS
# =============================================================================
# URL pública donde estará disponible tu backend (ej: https://api.tudominio.com)
BACKEND_PUBLIC_URL=https://your-backend-domain.com

# CORS - Dominios permitidos para admin, auth y store
ADMIN_CORS=https://your-backend-domain.com,https://your-storefront-domain.com
AUTH_CORS=https://your-backend-domain.com,https://your-storefront-domain.com
STORE_CORS=https://your-storefront-domain.com

# =============================================================================
# SECURITY SECRETS
# =============================================================================
# Genera secretos seguros para estos valores
JWT_SECRET=your_jwt_secret_here_minimum_32_characters
COOKIE_SECRET=your_cookie_secret_here_minimum_32_characters

# =============================================================================
# MEDUSA CONFIGURATION
# =============================================================================
# Clave publicable de Medusa (se genera después del primer despliegue)
MEDUSA_PUBLISHABLE_KEY=pk_your_medusa_publishable_key

# =============================================================================
# FILE STORAGE - MINIO
# =============================================================================
MINIO_ACCESS_KEY=your_minio_access_key
MINIO_SECRET_KEY=your_minio_secret_key_minimum_8_characters
MINIO_BUCKET=medusa-media

# =============================================================================
# EMAIL CONFIGURATION - RESEND
# =============================================================================
# Configuración para envío de emails (opcional)
RESEND_API_KEY=re_your_resend_api_key
RESEND_FROM_EMAIL=noreply@yourdomain.com

# =============================================================================
# EMAIL CONFIGURATION - SENDGRID (Alternativa a Resend)
# =============================================================================
# Solo usar si no usas Resend
# SENDGRID_API_KEY=SG.your_sendgrid_api_key
# SENDGRID_FROM_EMAIL=noreply@yourdomain.com

# =============================================================================
# PAYMENT CONFIGURATION - STRIPE
# =============================================================================
# Configuración de pagos con Stripe (opcional)
STRIPE_API_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret

# =============================================================================
# SEARCH CONFIGURATION - MEILISEARCH
# =============================================================================
# Configuración para búsqueda (opcional pero recomendado)
MEILISEARCH_ADMIN_KEY=your_meilisearch_master_key_minimum_16_characters

# =============================================================================
# NOTAS IMPORTANTES:
# =============================================================================
# 1. Cambia TODOS los valores de ejemplo por valores reales y seguros
# 2. Los secretos deben ser aleatorios y de al menos 32 caracteres
# 3. Las URLs deben coincidir con los dominios configurados en Coolify
# 4. El MEDUSA_PUBLISHABLE_KEY se genera automáticamente en el primer despliegue
# 5. Stripe y email son opcionales, pero recomendados para producción
